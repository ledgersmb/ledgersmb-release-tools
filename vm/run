#!/bin/bash


./build

virt-install \
    --import \
    --name releaser \
    --ram 1024 \
    --graphics vnc \
    --disk path=./tmp/debian-9.qcow2,bus=virtio \
    --network default,model=virtio \
    --boot hd \
    --os-type=generic

virsh destroy releaser
virsh undefine release
